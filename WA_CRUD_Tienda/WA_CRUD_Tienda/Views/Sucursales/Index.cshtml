@model IEnumerable<WA_CRUD_Tienda.Models.Sucursale>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DireccionSucursal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SucursalOculta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DireccionSucursal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SucursalOculta)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CodigoSucursal">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CodigoSucursal">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CodigoSucursal">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
